apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-api-service.fullname" . }}
  labels:
    {{- include "ai-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-api-service.selectorLabels" . | nindent 4 }}
