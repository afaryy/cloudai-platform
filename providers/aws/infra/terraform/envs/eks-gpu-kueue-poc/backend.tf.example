terraform {
  backend "s3" {
    bucket         = "REPLACE_WITH_BACKEND_BUCKET"
    key            = "cloudai-platform/eks-gpu-kueue-poc/terraform.tfstate"
    region         = "ap-southeast-2"
    dynamodb_table = "REPLACE_WITH_LOCK_TABLE"
    encrypt        = true
  }
}
