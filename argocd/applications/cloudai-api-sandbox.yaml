apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudai-api-sandbox
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: cloudai-api
    app.kubernetes.io/part-of: cloudai-platform
    cloudai-platform.example/owner: platform-engineering
    cloudai-platform.example/environment: sandbox
    cloudai-platform.example/data-scope: synthetic-only
    cloudai-platform.example/cost-center: portfolio
  annotations:
    cloudai-platform.example/release-boundary: synthetic-only
    cloudai-platform.example/sync-mode: manual
    cloudai-platform.example/rollback-runbook: docs/solutions/ai-release-engineering-on-eks.md
spec:
  project: default
  source:
    repoURL: https://github.com/afaryy/cloudai-platform.git
    targetRevision: main
    path: helm/ai-api-service
    helm:
      releaseName: cloudai-api
      values: |
        replicaCount: 1
        image:
          repository: hashicorp/http-echo
          tag: "1.0"
          pullPolicy: IfNotPresent
        containerArgs:
          - "-listen=:3000"
          - "-text=cloudai-api mock healthy"
        platformLabels:
          owner: platform-engineering
          environment: sandbox
          dataScope: synthetic-only
          costCenter: portfolio
          platformCapability: cloud-ai-platform
        config:
          mockMode: "true"
          providerMode: mock
          dataScope: synthetic-only
        podSecurityContext:
          runAsNonRoot: false
        securityContext:
          runAsNonRoot: false
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudai-sandbox
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - PruneLast=true
    retry:
      limit: 1
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 2m
